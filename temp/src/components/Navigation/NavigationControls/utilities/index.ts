import addOnResizeListener from './addOnResizeListener';
import {
  DEFAULT_LANGUAGE,
  DEVICE_BUTTON_SIZES_ENUM,
  DEFAULT_SCROLL_TIMEOUT,
  DEFAULT_TABLET_BREAKPOINT,
} from './defaults';

export {
  // Utilities
  addOnResizeListener,
  // Defaults
  DEFAULT_LANGUAGE,
  DEVICE_BUTTON_SIZES_ENUM,
  DEFAULT_SCROLL_TIMEOUT,
  DEFAULT_TABLET_BREAKPOINT,
};
