// Enums
const DEVICE_BUTTON_SIZES_ENUM = {
  tablet: 'medium',
  mobile: 'small',
  'small-mobile': 'small',
};

// Measurement
const DEFAULT_MOBILE_BREAKPOINT = 370;
const DEFAULT_TABLET_BREAKPOINT = 660;

// Timing
const DEFAULT_SCROLL_TIMEOUT = 500;

// Prop variables
const DEFAULT_LANGUAGE = {
  back: 'Back',
  begin: 'Begin',
  cancel: 'Cancel',
  complete: 'Complete',
  next: 'Next',
  of: 'of',
  percentageComplete: 'complete',
  total: 'Total',
};

export {
  DEFAULT_LANGUAGE,
  DEFAULT_MOBILE_BREAKPOINT,
  DEFAULT_SCROLL_TIMEOUT,
  DEFAULT_TABLET_BREAKPOINT,
  DEVICE_BUTTON_SIZES_ENUM,
};
