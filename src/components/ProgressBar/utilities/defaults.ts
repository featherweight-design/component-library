const DEFAULT_COMPLETION_COLOR = '#4bca81'; // $color-background-success from variables.scss
const DEFAULT_LANGUAGE = {
  complete: 'complete',
  of: 'of',
};
const DEFAULT_SIZE = 'medium';

const SIZES_ENUM = {
  'x-small': '0.125rem',
  small: '0.25rem',
  medium: '0.5rem',
  large: '0.75rem',
  'x-large': '1rem',
};

export { DEFAULT_COMPLETION_COLOR, DEFAULT_LANGUAGE, DEFAULT_SIZE, SIZES_ENUM };
